# 909 毕业纪念前端

React、TypeScript 与 Vite 构建的静态数字年鉴，包含三段核心影像、教师名言和 137 张照片档案。

## 本地开发

```powershell
npm ci
npm run dev
```

## 验证

```powershell
npm run lint
npm run test:run
npm run build
```

生产构建输出到 `dist/`，由 Cloudflare Pages 托管。
